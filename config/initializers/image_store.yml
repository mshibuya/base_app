development:
  reproxy:  false
  base_url: /images/
  mount_at: /images/
  storage:
    adapter: database
  cache:
    adapter: file_system

test:
  reproxy:  false
  base_url: 'http://image.example.com/images/'
  mount_at: /images/
  storage:
    adapter: database
  cache:
    adapter: file_system

